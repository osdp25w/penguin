apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: penguin
data:
  config.public.js: |
    // Public runtime config (non-sensitive)
    window.CONFIG = Object.assign(window.CONFIG || {}, {
      // Use koala.osdp25w.xyz for direct API calls
      API_BASE: 'https://koala.osdp25w.xyz'
    });
